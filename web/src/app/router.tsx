/* eslint-disable react-refresh/only-export-components */
import { Navigate, Outlet, createMemoryRouter, createBrowserRouter, type RouteObject } from 'react-router-dom'
import { ProtectedRoute } from './ProtectedRoute'; import { AppShell } from '../components/AppShell'; import { LoginPage } from '../features/auth/LoginPage'; import { OverviewPage } from '../pages/OverviewPage'; import { PolicyQaPage } from '../features/policy-qa/PolicyQaPage'; import { KnowledgeBasePage } from '../features/knowledge-base/KnowledgeBasePage'; import { InterviewDigestPage } from '../features/interview/InterviewDigestPage'; import { VoiceInsightPage } from '../features/voice/VoiceInsightPage'; import { WeeklyReportPage } from '../features/weekly/WeeklyReportPage'; import { CultureContentPage } from '../features/culture/CultureContentPage'; import { EvaluationPage } from '../features/evaluation/EvaluationPage'; import { SettingsPage } from '../features/settings/SettingsPage'
function ProtectedLayout() { return <ProtectedRoute><AppShell><Outlet /></AppShell></ProtectedRoute> }
export const routes: RouteObject[] = [{ path: '/login', element: <LoginPage /> }, { element: <ProtectedLayout />, children: [{ index: true, element: <OverviewPage /> }, { path: 'policy', element: <PolicyQaPage /> }, { path: 'knowledge', element: <KnowledgeBasePage /> }, { path: 'interview', element: <InterviewDigestPage /> }, { path: 'voice', element: <VoiceInsightPage /> }, { path: 'weekly', element: <WeeklyReportPage /> }, { path: 'culture', element: <CultureContentPage /> }, { path: 'evaluation', element: <EvaluationPage /> }, { path: 'settings', element: <SettingsPage /> }] }, { path: '*', element: <Navigate to="/" replace /> }]
export const createAppRouter = (initialEntries?: string[]) => initialEntries ? createMemoryRouter(routes, { initialEntries }) : createBrowserRouter(routes)
