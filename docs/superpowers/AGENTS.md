# 设计规格目录规范

`specs/` 存放已确认、可审阅的产品与交互设计规格。文件按
`YYYY-MM-DD-<topic>-design.md` 命名；只记录决策、接口约束与验收标准，
不放实现代码或临时草稿。规格变更应新建或修订原文件，并同步说明影响范围。

`reviews/` 预留给实现前或发布前的设计审查结果。设计截图、下载的参考图和
缓存不得提交到此目录；它们属于临时材料，应在审查结束后清理。
