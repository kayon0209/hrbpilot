"""MCP 工具的对外中文描述 —— 集中、版本化，不散落在装饰器里。

为什么单独一个文件
------------------
工具描述是**给外部 AI Agent 的路由依据**（模型靠它决定"这句话该调哪个工具"），
同时也是给评审/本地化的一个 artifact：改动它就是改动路由行为，应当像代码一样
可 diff、可回归，而不是埋在 ``server.py`` 的装饰器参数里十一次。

每条描述必须含三件事（任务书 T3-4）：

1. **何时用**：用户什么样的诉求对应这个工具；
2. **何时不用**：最容易混淆的相邻工具，写清边界（查制度用 ``search_policy``，
   不要用它查案件进度）；
3. **1–2 个真实中文例子**：外部模型按例子对齐口语说法，比抽象描述有效。

描述修订时改 ``TOOL_DESCRIPTIONS`` 并把 ``DESCRIPTIONS_VERSION`` 加一 ——
评测基线（T6）据此区分"描述改动前后"的路由数字。

写法约束：面向 HR / HR 经理的中文口语；不出现内部术语（tenant、scope 名、
表名）；「需要登录」统一保留 —— 它向客户端如实表达鉴权预期。
"""

from __future__ import annotations

#: 描述集版本。改任何一条描述都应 +1，供评测报告与回归对账。
DESCRIPTIONS_VERSION = "2026-09-15.1"

TOOL_DESCRIPTIONS: dict[str, str] = {
    "search_policy": (
        "【何时用】用户问公司制度、规定、流程怎么定的（年假、加班费、试用期、"
        "报销、请假审批等），需要给出制度依据和出处。\n"
        "【何时不用】用户要看某份制度的完整原文某一章 → 用 get_policy_source；"
        "用户问的是某个案件的进展 → 用 get_case_summary，本工具不查案件。\n"
        "【例子】「公司的年假是怎么规定的？」「试用期最长能多久？」\n"
        "【要求】回答时必须引用返回里的出处（制度名称与条款位置），检索不到就明说没有找到，"
        "不要凭常识补充。需要登录。"
    ),
    "get_policy_source": (
        "【何时用】用户点名要看某份制度文件的原文，或只要其中某一章。"
        "返回内容用于核对具体条款的完整措辞。\n"
        "【何时不用】用户只有一个问题、不知道文件名 → 先用 search_policy；"
        "需要摘要或答案而非原文时也不必调本工具。\n"
        "【例子】「把《员工手册》第四章的原文给我」「调出加班费规定的原文看看」。\n"
        "【要求】需要登录。"
    ),
    "search_cases": (
        "【何时用】用户要找自己有权查看的人事案件：按状态、类别过滤，或列最近案件。"
        "所有「对案件做动作」的前提 —— 没有案件编号时先用它。\n"
        "【何时不用】用户已经知道案件编号、要看详情 → 用 get_case_summary；"
        "查制度规定 → 用 search_policy，本工具不返回制度内容。\n"
        "【例子】「我有权限看哪些案件？」「列一下最近的高风险案件」。\n"
        "【要求】只返回编号、标题、类别、风险等级与状态，不含员工身份信息。需要登录。"
    ),
    "get_case_summary": (
        "【何时用】用户想了解某个案件的详细情况（背景、进展、审批记录），"
        "通常在 search_cases 之后、或用户点名了某个案件时使用。\n"
        "【何时不用】只问审批到哪一步 → 用 get_approval_status 更直接；"
        "查制度 → 用 search_policy。\n"
        "【例子】「把刚才那个案件的详细情况说给我」「王五那件事到哪一步了」"
        "（先解析出案件编号）。\n"
        "【要求】看不见的案件与不存在的案件返回同样的结果 —— 这是刻意的，不是故障。需要登录。"
    ),
    "get_approval_status": (
        "【何时用】用户问「刚才提交的那个动作批了没」「这个案件的审批现在什么状态」。"
        "必须提供案件编号。\n"
        "【何时不用】审批编号不能单独作为查询依据；案件全貌 → get_case_summary。\n"
        "【例子】「刚才提交的跟进任务现在什么状态？」「这个案件审批过了吗？」\n"
        "【要求】回答状态要引用返回的真实状态，不要凭印象。需要登录。"
    ),
    "get_my_access_profile": (
        "【何时用】用户问「我这个连接都能干什么」「我能查制度吗」「为什么某个工具用不了」，"
        "或需要向用户说明当前权限时。\n"
        "【何时不用】不要用它查业务内容（制度/案件）——它只返回你自己的身份与可用范围。\n"
        "【例子】「告诉我当前连接身份能使用哪些能力」「我有没有权限看案件？」\n"
        "【要求】只返回你自己的信息，不含其他用户数据。"
    ),
    "create_hr_case": (
        "【何时用】用户要为某位员工登记一条新的人事案件（试用期异常、离职面谈、"
        "投诉受理等）。\n"
        "【何时不用】给已有案件建待办 → create_work_task；查询 → search_cases。\n"
        "【例子】「帮 EMP-001 建一条试用期异常跟进」。\n"
        "【要求】只创建待审批记录，HR 批准后才生效。需要登录并提供案件编号。"
    ),
    "assign_case_owner": (
        "【何时用】用户要把某个案件转给指定负责人跟进。\n"
        "【何时不用】改案件状态 → update_case_status；只是通知 → send_case_notification。\n"
        "【例子】「把这个案件转给李经理负责」。\n"
        "【要求】只创建待审批记录，HR 批准后才生效。需要登录并提供案件编号。"
    ),
    "send_case_notification": (
        "【何时用】用户要就某个案件给相关同事发一条站内通知。\n"
        "【何时不用】要改案件本身（负责人/状态）不是本工具；对外部渠道发送不支持。\n"
        "【例子】「给这个案件的相关人发个站内通知，提醒材料还没交齐」。\n"
        "【要求】只创建待审批记录，HR 批准后才真正发送。需要登录并提供案件编号。"
    ),
    "update_case_status": (
        "【何时用】用户要把某个案件标记为已解决（或按接口约定变更状态）。\n"
        "【何时不用】转给他人 → assign_case_owner；只是加待办 → create_work_task。\n"
        "【例子】「这个案件处理完了，帮我标记为已解决」。\n"
        "【要求】只创建待审批记录，HR 批准后才生效。需要登录并提供案件编号。"
    ),
    "create_work_task": (
        "【何时用】用户要给某个案件挂一条跟进待办，写清下一步做什么、谁来跟、"
        "什么时候截止。\n"
        "【何时不用】新建案件本身 → create_hr_case；只查待办不属于 MCP 能力范围。\n"
        "【例子】「帮王五的案件建一个补齐入职材料的跟进任务」。\n"
        "【要求】只创建待审批记录，HR 批准后才生效。需要登录并提供案件编号。"
    ),
}
