# HRBPilot 设计系统基线

> 版本：1.0（对应正式产品 UI/UX 最终实施方案 §十二）
> 本文件是前端实现的唯一视觉与交互规范。任何页面样式与本文件冲突时，以本文件为准。

## 1. 视觉方向

克制的任务型 App UI。安静表面、清楚层级、少量强调色。

**禁止**：营销页式 hero、品牌口号、卡片拼贴、AI 渐变、渐变按钮、礼花/金币等装饰动画。

## 2. 字体

| 角色 | 字体 | 规则 |
|---|---|---|
| Display | Noto Serif SC 500–700 | 页面标题、阶段完成、关键总结；每屏最多两处 |
| Body/UI | Noto Sans SC 400–700 | 正文、表单、按钮、导航、表格 |
| Data | IBM Plex Mono 500 | ID、时间戳、版本和管理员技术详情 |

- 生产环境使用 `web/public/fonts` 内自托管的 Noto SC WOFF2 分片，运行时不访问 Google Fonts。
- 字体按 SIL Open Font License 1.1 分发，许可文件为 `web/public/fonts/OFL.txt`。
- 回退栈：正文 → HarmonyOS Sans SC, Microsoft YaHei UI, PingFang SC, sans-serif；标题 → Source Han Serif SC, Songti SC, serif。

字号 / 行高（px）：

```
display 32/42 · h2 24/34 · h3 18/28 · body 16/26 · ui 14/22 · meta 13/20 · micro 12/18
```

- 正文 16px；UI 控件 14px；micro（12px）不得承载关键说明。
- 长文阅读区最大宽度 760px。

## 3. 色彩

| Token | 值 | 用途 |
|---|---|---|
| `canvas` | `#F6F6F1` | 全局背景 |
| `surface` | `#FFFFFF` | 主工作面 |
| `surface-subtle` | `#F0F1EC` | 次级分区 |
| `ink` | `#1E2420` | 主文字 |
| `ink-soft` | `#545C55` | 次级文字 |
| `muted` | `#737B74` | 时间和弱元信息 |
| `line` / `line-strong` | `#E1E4DD` / `#C7CDC4` | 分隔和控件边界 |
| `action` / `action-hover` | `#1F5A48` / `#184939` | 主动作 |
| `action-soft` | `#E3EEE8` | 选中和轻提示 |
| `evidence` / `evidence-soft` | `#A8642F` / `#F4EADF` | 证据和来源（不得挪作装饰） |
| `success` | `#2F6B50` on `#E5F0E9` | 完成 |
| `warning` | `#93611E` on `#F6EBD8` | 等待、到期、部分完成 |
| `danger` | `#B4473D` on `#F7E7E5` | 失败和高影响动作 |
| `info` | `#496B7A` on `#E6EEF1` | 中性说明 |

规则：
- 颜色不能单独承载状态——必须同时有文字或图标。
- 对比度：正文与关键文字 ≥ 4.5:1；焦点与相邻状态 ≥ 3:1。
- 深色模式不在当前范围。

## 4. 尺寸与布局

- 4px 基础单位；间距限定 2、4、8、12、16、20、24、32、40、48、64。
- 控件默认高 40px；触控目标包围盒 ≥ 44×44px。
- 圆角 4/6/8px；pill（999px）仅用于头像和短状态。
- 普通列表、表格、对象**不使用阴影**；阴影仅用于弹层、抽屉、菜单。
- 桌面侧栏 232px、顶部栏 56px、主内容最大 1240px。
- 阅读正文最大 760px；证据区 320px；清单辅助区 320–360px。
- 卡片只有在卡片本身是可操作对象时存在。

## 5. 动效

- hover/focus 100ms；状态切换 180ms；抽屉 260ms。
- 仅解释状态变化和空间关系；支持 `prefers-reduced-motion`。
- 禁止循环装饰动画和无意义动态图。

## 6. 图表

- 必须有真实分母、时间范围、数据新鲜度，并能进入对象清单。
- 经理可用趋势/构成/任务分布；HRBP 首页不放装饰图表；技术图表只在管理后台。
- 第二份可比较数据出现前不显示空趋势图。

## 7. 响应式断点

- `≥1200px`：主工作区 + 辅助上下文区。
- `820–1199px`：辅助区改抽屉，主任务保持完整宽度。
- `<820px`：导航抽屉、单列、当前主动作可固定底部。
- 验收视口：390×844、768×1024、1280×800、1440×900；200% 缩放不丢关键动作、不产生双向滚动。

## 8. 可访问性基线（WCAG 2.2 AA）

- 主流程 100% 键盘可达，无键盘陷阱。
- 2px 可见焦点；焦点不被固定栏/弹层遮住。
- 动态回答、保存结果、错误使用 `aria-live`（不逐 token 播报）。
- 抽屉/对话框：打开移入焦点、限制范围、Esc 关闭、关闭后回到触发点。
- 表单持久可见标签；不用 placeholder 代替标签。
- 图标按钮有可访问名称；状态不只依赖颜色/图标。
- 拖拽必须有按钮替代；自动刷新不夺焦点。
- 登录支持密码管理器和粘贴。

## 9. 命名与文案

界面用业务语言：数据接入 / 数据来源 / AI 质量 / 系统状态。
**禁止出现**：connector、MCP、endpoint、沙箱、检索延迟、confidence 百分比、模型名、token。

文案只承担：方向、状态、原因、影响、动作。空状态必须含原因 + 主动作，禁止只写"暂无数据"。

## 10. 状态语义

| 场景 | 展示 |
|---|---|
| 单次生成（无可测步骤） | 排队中 → 正在分析 → 已完成 / 失败 |
| 有业务阶段无总量 | 阶段条：当前阶段、下一阶段、负责人、等待对象 |
| 有真实总量 | 已完成 x/y、百分比、失败数 |

禁止为每个任务强行绘制进度条；禁止无真实分母的百分比。
